<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://chris-pierce.com/feed.xml" rel="self" type="application/atom+xml"/><link href="https://chris-pierce.com/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-09-28T02:11:18+00:00</updated><id>https://chris-pierce.com/feed.xml</id><title type="html">blank</title><subtitle>Scientist studying the intersection of accelerator physics and machine learning. </subtitle><entry><title type="html">Sampling from the 2D Supergaussian Distribution</title><link href="https://chris-pierce.com/blog/2025/sampling-from-supergaussian/" rel="alternate" type="text/html" title="Sampling from the 2D Supergaussian Distribution"/><published>2025-09-24T15:12:00+00:00</published><updated>2025-09-24T15:12:00+00:00</updated><id>https://chris-pierce.com/blog/2025/sampling-from-supergaussian</id><content type="html" xml:base="https://chris-pierce.com/blog/2025/sampling-from-supergaussian/"><![CDATA[<p><img src="/assets/img/tech_notes/supergaussian_samples_3.png" alt="Supergaussian density" width="400"/></p> <p>The 2D supergaussian distribution is useful for describing electron beams generated from lasers that can vary from Gaussian to flat-top in shape. To simulate these beams in a particle tracking code, samples I need a method to generate samples from this distribution numerically. In this note, I derive formulas to do just that using inversion sampling.</p> <h1 id="inversion-sampling">Inversion Sampling</h1> <p>A neat, efficient way of generating samples from an arbitrary distribution is inversion sampling. First, we generate samples over $[0, 1]$ from the uniform distribution ($\rho_u$). Then, we transform them by the inverse of the cummulative density function (CDF) of the desired distribution ($\rho_d$). Let’s calculate the resulting density of the transformed samples knowing that densities transform with the determinant of the Jacobian out front.</p> \[\rho_u(x) \to \left[ \frac{d [\text{CDF}(x^\prime)]}{d x^\prime}\right]_{x^\prime}\cdot \rho_u(\text{CDF}(x^\prime)).\] <p>We can simplfy this. First off, $\rho_u(\text{CDF}^{-1}(x^\prime))$ is one everywhere since we began with the uniform distribution which is $\rho_u(x) = 1$ over $[0, 1]$ and the CDF maps from the domain of our desired distribution onto $[0, 1]$. The second simplification is that the CDF is just the integral of the PDF of our desired distribution. This means $\left[ \frac{d [\text{CDF}]}{d x^\prime}\right]_{x^\prime} = \rho_d(x’)$. Therefore, the transformed density is,</p> \[\rho_u(x) \to \rho_d(x^\prime).\] <p>We can therefore create samples from the desired distribution by drawing samples from the uniform distribution over $[0, 1]$ and applying the transformation $\text{CDF}^{-1}(x)$. Note that unlike rejection sampling, every sample we begin with ends up in the final samples. However, we must be able to evaluate $\text{CDF}^{-1}(x)$ efficiently.</p> <h1 id="inverse-cdf-of-supergaussian">Inverse CDF of Supergaussian</h1> <p>There are multiple ways of defining the supergaussian distribution, but for this note I will use the convention,</p> \[\rho(x, y) = A\cdot\exp\left[-\log(2)\left(4x^2 + 4y^2\right)^n\right].\] <p>This variant is symmetric in $x$ and $y$ and has a full width at half maximum of one. I don’t include variance/covariance as parameters here as they can be trivially applied to the resulting samples through a linear transformation after generation. A figure showing the density for different values of $n$ with $y=0$ is shown at the top of this note.</p> <p>Note that a transformation to polar coordinates naturally expresses the rotational symmetry of the density. Doing this and being careful to multiply by the determinant of the Jacobian to preserve the interpretation of $\rho$ as a probability density function (PDF) gives,</p> \[\rho(r, \theta) = |J|\cdot\rho(x(r, \theta), y(r, \theta)) = A\cdot r\cdot\exp\left[-\log(2)4^nr^{2n}\right].\] <p>Since the PDF is separable, we can draw samples for $\theta$ (uniformly distributed) and $r$ (distributed accoring to the above equation) separately, then transform into cartesian coordinates. To use inversion sampling for $r$, we must calculate the inverse of its CDF</p> <p>Integrating the previous equation and then setting $A$ so that $\lim\limits_{r\to\infty} \text{CDF}(r) = 1$ gives,</p> \[\text{CDF}(r) = 1 - \frac{\Gamma(1/n, 4^nr^{2n}\log(2))}{\Gamma(1/n)},\] <p>where $\Gamma(x)$ and $\Gamma(a, x)$ are the complete and upper incomplete gamma functions respectively.</p> <p>This CDF isn’t invertible in terms of common functions. However, many popular numerical libraries (such as <a href="https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.invgamma.html">scipy</a> and <a href="https://www.boost.org/doc/libs/1_64_0/libs/math/doc/html/math_toolkit/sf_gamma/igamma_inv.html">boost</a>) contain methods to efficiently calculate the inverse of the incomplete Gamma functions. Solving the problem in terms of the inverse regularized gamma function (I use the convention of <code class="language-plaintext highlighter-rouge">gamma_q_inv</code> from boost), the inverse CDF is,</p> \[\text{CDF}^{-1}(u) = \frac{1}{2}\left[\Gamma_q^{-1}(1/n, 1-u)/\log(2)\right]^{1/(2n)}.\] <h2 id="generating-samples-in-python">Generating Samples in Python</h2> <p>To recap, I have calculated the inverse CDF of the radial part of the supergaussian distribution in polar coordinates. To generate a sample, we first generate two uniformly distributed random numbers, the first is transformed by the inverse of the CDF we found to get $r$. The second is rescaled to $[0, 2\pi]$ to give us $\theta$. The coordinates are the then transformed to cartesian coordinates.</p> <p>The following python code implements this.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">scipy.special</span> <span class="kn">import</span> <span class="n">gammainccinv</span>
<span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="k">def</span> <span class="nf">sample_supergaussian</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="n">n_samp</span><span class="p">):</span>
    <span class="c1"># Start with uniform random samples
</span>    <span class="n">u</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">random</span><span class="p">(</span><span class="n">n_samp</span><span class="p">)</span>
    <span class="n">v</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">random</span><span class="p">(</span><span class="n">n_samp</span><span class="p">)</span>

    <span class="c1"># Transform by inverse CDF of dist in polar coordinates
</span>    <span class="n">theta</span> <span class="o">=</span> <span class="mi">2</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="n">pi</span><span class="o">*</span><span class="n">u</span>
    <span class="n">r</span> <span class="o">=</span> <span class="mf">0.5</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="nf">power</span><span class="p">(</span><span class="nf">gammainccinv</span><span class="p">(</span><span class="mi">1</span><span class="o">/</span><span class="n">n</span><span class="p">,</span> <span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="n">v</span><span class="p">))</span><span class="o">/</span><span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="mi">2</span><span class="p">),</span> <span class="mi">1</span><span class="o">/</span><span class="mi">2</span><span class="o">/</span><span class="n">n</span><span class="p">)</span>

    <span class="c1"># Transform to Cartesian coordinates
</span>    <span class="n">x</span> <span class="o">=</span> <span class="n">r</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="n">theta</span><span class="p">)</span>
    <span class="n">y</span> <span class="o">=</span> <span class="n">r</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="n">theta</span><span class="p">)</span>

    <span class="k">return</span> <span class="n">x</span><span class="p">,</span> <span class="n">y</span>
</code></pre></div></div> <p>This code generates the following samples. Remember that in my convention, $n=1$ is a Gaussian distribution, $n\to\inf$ is flat-top.</p> <p><img src="/assets/img/tech_notes/supergaussian_samples_1.png" alt="Supergaussian samples" width="500"/></p> <p>The samples behave qualitatively as expected. Plotting the densities using a histogram also helps to confirm that the method works.</p> <p><img src="/assets/img/tech_notes/supergaussian_samples_2.png" alt="Supergaussian density" width="500"/></p> <h2 id="miscellaneous-notes">Miscellaneous Notes</h2> <ul> <li>The PDF is also separable in Cartesian coordinates, but will give a similar CDF in terms of gamma functions. Generating the samples in polar coordinates reduces the number of calls to the inverse gamma function by a factor of two (only one for $r$ instead of one each for $x$ and $y$) which is desirable because it is the most computationally expensive part of the process.</li> </ul>]]></content><author><name></name></author><summary type="html"><![CDATA[Formulas for drawing samples from the supergaussian distribution with inversion sampling.]]></summary></entry></feed>